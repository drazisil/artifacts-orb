description: >
  Prepare and set artifacts directory path

usage:
  version: 2.1
  orbs:
    artifacts: drazisil/artifacts@dev:alpha
  jobs:
    build:
      machine: true
      steps:
        - artifacts/prepare-artifacts-dir
